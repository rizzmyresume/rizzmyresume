export async function saveProfile(profile) {
  // Placeholder to integrate with Supabase or another DB later
  console.log("Saving profile", profile);
  return profile;
}

export async function getProfile() {
  // Placeholder fetch
  return null;
}
